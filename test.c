#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, char **argv){
	time_t t = time(NULL);
	printf("Test number: %d %d %d %d\n", sizeof(struct heap_free_area), 2, 0, __alignof__(T));
	return 0;
}